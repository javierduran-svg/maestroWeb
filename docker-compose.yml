services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: maestroweb
      POSTGRES_PASSWORD: maestroweb
      POSTGRES_DB: maestroweb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
